var heart = document.querySelector(".heart")

heart.innerHTML =
        `<%= link_to user_movie_favorite_path(current_user, @movie, @favorite), method: :delete, remote:true do  %>
          <i class="fas fa-heart"></i>
        <% end %>`
